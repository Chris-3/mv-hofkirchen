import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'mvhofki.xyz',
  appName: 'mv-hofkirchen',
  webDir: 'dist/mv-hofkirchen',
  bundledWebRuntime: false
};

export default config;
